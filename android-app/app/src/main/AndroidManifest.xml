<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml — Comfort Zone TWA (Trusted Web Activity) Wrapper

    This manifest wraps the Comfort Zone PWA (https://ssewasswa.onrender.com)
    as a native Android app using Google's Trusted Web Activity API.

    TWA uses Chrome's rendering engine — NOT a WebView — so the PWA
    features (offline, push notifications, service worker) all work.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ComfortZone.Splash"
        android:usesCleartextTraffic="false">

        <!-- TWA Launcher Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|keyboard|keyboardHidden"
            android:theme="@style/Theme.ComfortZone.Splash">

            <!-- App icon launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Android App Links — verifies ownership of ssewasswa.onrender.com -->
            <!-- This requires the assetlinks.json file to be served at:
                 https://ssewasswa.onrender.com/.well-known/assetlinks.json -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:host="ssewasswa.onrender.com" />
            </intent-filter>

            <!-- Splash screen metadata for TWA -->
            <meta-data
                android:name="android.webkit.webview.AssetLoader"
                android:value="" />

        </activity>

        <!-- Trusted Web Activity support via Chrome Custom Tabs -->
        <activity
            android:name="androidx.browser.trusted.LauncherActivity"
            android:exported="false">
            <meta-data
                android:name="android.support.customtabs.trusted.DEFAULT_URL"
                android:value="https://ssewasswa.onrender.com" />
            <meta-data
                android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR"
                android:resource="@color/primary" />
            <meta-data
                android:name="android.support.customtabs.trusted.NAVIGATION_BAR_COLOR"
                android:resource="@color/primary" />
            <meta-data
                android:name="android.support.customtabs.trusted.SPLASH_IMAGE_DRAWABLE"
                android:resource="@drawable/splash_background" />
            <meta-data
                android:name="android.support.customtabs.trusted.SCREEN_ORIENTATION"
                android:value="unspecified" />
        </activity>

    </application>

</manifest>
